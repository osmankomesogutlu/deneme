import 'package:flutter/material.dart';

class SmalAdresWidget extends StatelessWidget {
  const SmalAdresWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('') ,
    );
  }
}