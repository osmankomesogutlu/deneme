import 'package:flutter/material.dart';

class Favoriler extends StatelessWidget {
  const Favoriler({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Şuanlık tatilde'),
    );
  }
}